# Environment variables (contain real secrets — never commit)
.env
.env.*
!.env.example
"- Copie.env"

# Build artifacts / dependencies (root frontend)
node_modules/
dist/

# Backend-served frontend bundle — regenerated by render.yaml's buildCommand
backend/public/index.html
